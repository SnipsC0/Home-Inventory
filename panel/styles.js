// Opțional, poți muta/adauga reguli comune aici
export function setupGlobalStyles(root) {
  // exemplu: nimic critic, dar ai loc pentru stiluri comune
  // dacă vrei shadow DOM, poți migra pe attachShadow și să inserezi un <style> acolo
}
